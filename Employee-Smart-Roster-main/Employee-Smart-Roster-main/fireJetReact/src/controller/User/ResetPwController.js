const fetch = require('node-fetch').default;

function handleSendResetPwURL(email) {
    // Trigger check user email and send url API here

    // return response code
}

export default {
    handleSendResetPwURL,
}